import 'package:flutter/material.dart';

class AllSubscription extends StatelessWidget {
  const AllSubscription({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("AllSubscription"),
      ),
    );
  }
}
