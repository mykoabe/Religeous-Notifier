import 'package:flutter/material.dart';

class AllSchedule extends StatelessWidget {
  const AllSchedule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("AllSchedule"),
      ),
    );
  }
}
